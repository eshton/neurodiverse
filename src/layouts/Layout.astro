---
import '../styles/global.css';
import SiteHeader from '../components/SiteHeader.astro';
import Seo from '../components/Seo.astro';
import { ClientRouter } from 'astro:transitions';

interface Props {
  title: string;
  locale?: string;
  description?: string;
  image?: string;
}
const { title, description, image } = Astro.props;
---
<!doctype html>
<html lang="hu" class="scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
    <Seo title={title} description={description} image={image} />
    <ClientRouter />
    <script is:inline>
      // Apply dark mode on first paint AND re-apply after each View Transitions
      // swap: the incoming page's <html> attributes replace the live ones, which
      // would otherwise drop the `dark` class and flash the site to light.
      const applyTheme = () => {
        if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
          document.documentElement.classList.add('dark');
        }
      };
      applyTheme();
      document.addEventListener('astro:after-swap', applyTheme);
    </script>
  </head>
  <body class="bg-white text-zinc-800 dark:bg-zinc-950 dark:text-zinc-100 min-h-screen flex flex-col">
    <SiteHeader />

    <main class="max-w-4xl mx-auto px-4 py-10 flex-1 w-full">
      <slot />
    </main>

    <footer class="border-t border-zinc-200 dark:border-zinc-800 mt-auto">
      <div class="max-w-4xl mx-auto px-4 py-6 text-sm text-zinc-500 dark:text-zinc-400">
        Neurodiverse. ADHD és autizmus források.
      </div>
    </footer>
  </body>
</html>
