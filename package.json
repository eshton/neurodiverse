{
  "name": "neuropont",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Neuropont — a curated, location-aware directory of ADHD & autism resources in Hungary (map, browsable web grid, and an open committed dataset). Static Astro site; content is markdown, git is the datastore.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "astro dev",
    "build:db": "node scripts/build-db.mjs",
    "prebuild": "node scripts/build-db.mjs",
    "predev": "node scripts/build-db.mjs",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.6",
    "tailwindcss": "^4.3.2"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "yaml": "^2.9.0"
  }
}
